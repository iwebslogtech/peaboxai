- name: Setup Python
  uses: actions/setup-python@v7.0.0
  with:
    # Version range or exact version of Python or PyPy to use, using SemVer's version range syntax. Reads from .python-version if unset.
    python-version: # optional
    # File containing the Python version to use. Example: .python-version
    python-version-file: # optional
    # Used to specify a package manager for caching in the default directory. Supported values: pip, pipenv, poetry.
    cache: # optional
    # The target architecture (x86, x64, arm64) of the Python or PyPy interpreter.
    architecture: # optional
    # Set this option if you want the action to check for the latest available version that satisfies the version spec.
    check-latest: # optional
    # The token used to authenticate when fetching Python distributions from https://github.com/actions/python-versions. When running this action on github.com, the default value is sufficient. When running on GHES, you can pass a personal access token for github.com if you are experiencing rate limiting.
    token: # optional, default is ${{ github.server_url == 'https://github.com' && github.token || '' }}
    # Used to specify the path to dependency files. Supports wildcards or a list of file names for caching multiple dependencies.
    cache-dependency-path: # optional
    # Set this option if you want the action to update environment variables.
    update-environment: # optional, default is true
    # When 'true', a version range passed to 'python-version' input will match prerelease versions if no GA versions are found. Only 'x.y' version range is supported for CPython.
    allow-prereleases: # optional
    # When 'true', use the freethreaded version of Python.
    freethreaded: # optional
    # Used to specify the version of pip to install with the Python. Supported format: major[.minor][.patch].
    pip-version: # optional

- name: Dependencies Checker
  # You may pin to the exact commit or the version.
  # uses: matheusjardimb/dependencies-checker@1ed9fbeb40e239a7dc833f189a73964799839e44
  uses: matheusjardimb/dependencies-checker@v1.1.3
  with:
    # Complete path of the package.json file to be evaluated
    packageJsonPath: # optional, default is package.json
    # Reduces the amount of logs printed during evaluation.
    quiet: # optional

- name: Compile and PR Python Requirements
  # You may pin to the exact commit or the version.
  # uses: barseghyanartur/compile-python-requirements-action@4e5699f382ce7376e87b1a074a18f6656f74cd3e
  uses: barseghyanartur/compile-python-requirements-action@0.1
  with:
    # Input file (requirements.in)
    input-file: 
    # Operating system name (platform slug)
    os-name: 
    # GitHub Token for creating pull requests and committing
    github-token: # optional, default is 
    # Directory to place the compiled requirements
    output-directory: # optional, default is requirements/
    # Target branch
    target-branch: # optional, default is main
    # Prefix for the output file
    prefix: # optional, default is 
    # Flag to determine if an artifact should be created
    create-artifact: # optional, default is false
    # Flag to determine if debug mode
    debug: # optional, default is false
    
Run tests later when tests exist
